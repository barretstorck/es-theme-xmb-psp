<?xml version="1.0" encoding="UTF-8"?>
<!--
  es-theme-xmb-psp - system view (PSP XMB top row + animated wave background).
  CC-BY-NC-SA 2.0. See CREDITS.md.
-->
<theme>
  <formatVersion>7</formatVersion>

  <view name="system">

    <!-- Wave background. Center-anchored, 10% over-scanned so storyboard
         motion (x/scale) stays within the visible frame without
         revealing PNG edges. Marked extra="true" to render; the
         documented <image name="background"> slot without extra="true"
         did not render in this ES build. Side effect: extras in system
         view scroll with the carousel, so wave appears to reset on
         system change. Accepted v0.3 limitation. -->
    <image name="waveBackground" extra="true">
      <path>./art/wave/wave.png</path>
      <pos>0.5 0.5</pos>
      <size>1.10 1.10</size>
      <origin>0.5 0.5</origin>
      <color>${waveTint}</color>
      <zIndex>0</zIndex>
    </image>

    <!-- Top-right clock, PSP-style (24h, time only). -->
    <datetime name="clock" extra="true">
      <pos>0.88 0.03</pos>
      <size>0.10 0.04</size>
      <fontPath>${fontRegular}</fontPath>
      <fontSize>0.030</fontSize>
      <color>${textPrimary}</color>
      <format>%H:%M</format>
      <horizontalAlignment>right</horizontalAlignment>
      <zIndex>10</zIndex>
    </datetime>

    <!-- Horizontal carousel of system icons (PSP "category row"), raised to the y=0.33 XMB cross bar. -->
    <carousel name="systemcarousel">
      <defaultTransition>fade</defaultTransition>
      <type>horizontal</type>
      <!-- Box top y=0.18, height 0.30 -> bar centerline y=0.33 (= crossY).
           Fallback: container shifted so its center (the selection point)
           lands at x=0.33, i.e. -17% left of screen-center. logoPos was
           tried first but made the carousel fail to render on this build. -->
      <pos>-0.17 0.18</pos>
      <size>1 0.30</size>
      <color>00000000</color>
      <logoSize>0.10 0.14</logoSize>
      <logoScale>1.5</logoScale>
      <logoRotation>0</logoRotation>
      <logoAlignment>center</logoAlignment>
      <maxLogoCount>7</maxLogoCount>
      <textColor>${textSecondary}</textColor>
      <selectedTextColor>${textPrimary}</selectedTextColor>
      <zIndex>5</zIndex>
    </carousel>

    <!-- systemInfo: the carousel-managed name/count element. Positioned here
         so the gamecount subset can control its visibility/delay.
         The carousel engine populates its text; our <text> override is
         ignored by the carousel slot binding. -->
    <text name="systemInfo">
      <pos>${crossX} ${captionY}</pos>
      <origin>0.5 0</origin>
      <size>0.5 0.045</size>
      <fontPath>${fontLight}</fontPath>
      <fontSize>0.025</fontSize>
      <color>${textSecondary}</color>
      <backgroundColor>00000000</backgroundColor>
      <alignment>center</alignment>
      <zIndex>6</zIndex>
    </text>

    <!-- System-name caption: extra text element bound to the selected
         system's display name via ${system.fullName}. Pinned to the same
         crossX/captionY as the carousel (so both overlay). The gamecount
         Hide subset keeps this visible; Show subset hides it so the
         carousel's own count cycling (systemInfo) can show through.
         Note: extra="true" is required to render in system view on this
         build; this inherits the documented carousel-scroll quirk (caption
         slides during system-change transition), but content updates
         correctly per selected system. -->
    <text name="systemName" extra="true">
      <pos>${crossX} ${captionY}</pos>
      <origin>0.5 0</origin>
      <size>0.5 0.045</size>
      <fontPath>${fontLight}</fontPath>
      <fontSize>0.025</fontSize>
      <color>${textSecondary}</color>
      <backgroundColor>00000000</backgroundColor>
      <alignment>center</alignment>
      <zIndex>7</zIndex>
      <text>${system.fullName}</text>
    </text>

    <!-- Per-system icon. Knulli's ES uses 'logo' as the element name
         (not 'system-logo') and feeds it into the carousel slots.
         ES substitutes ${system.theme} with the current system shortname. -->
    <image name="logo">
      <path>./art/system-icons/${system.theme}.png</path>
      <maxSize>0.12 0.16</maxSize>
      <zIndex>6</zIndex>
    </image>

  </view>
</theme>
