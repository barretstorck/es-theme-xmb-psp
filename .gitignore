# dev workflow scratch
.dev/

# local config overrides (may contain SSHPASS)
.env.local

# Claude Code harness session state
.claude/

# Python bytecode (scripts/*.py are run directly, never imported as a package)
__pycache__/
*.pyc

# macOS
.DS_Store

# editor swap files
*.swp
*~

# SDD scratch workspace
.superpowers/

# cached upstream batocera es_systems.yml (make-library-systems.py fetches it)
scripts/es_systems.yml.cache
